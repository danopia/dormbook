<%= form_for(@zebra) do |f| %>
  <% if @zebra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@zebra.errors.count, "error") %> prohibited this zebra from being saved:</h2>

      <ul>
      <% @zebra.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :roomie_id %><br />
    <%= f.text_field :roomie_id %>
  </div>
  <div class="field">
    <%= f.label :session_id %><br />
    <%= f.text_field :session_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
